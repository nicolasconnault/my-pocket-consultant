import { CONSULTANT_MODE_COLOR } from './config'

const uiTheme = {
  palette: {
    primaryColor: CONSULTANT_MODE_COLOR,
  },
  toolbar: {
    container: {
      height: 50,
    },
  },
}
export default uiTheme
