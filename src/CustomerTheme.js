import { CUSTOMER_MODE_COLOR } from './config'

const uiTheme = {
  palette: {
    primaryColor: CUSTOMER_MODE_COLOR,
  },
  toolbar: {
    container: {
      height: 50,
    },
  },
}

export default uiTheme
